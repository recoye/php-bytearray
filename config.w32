// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("bytearray", "for bytearray support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("bytearray", "enable bytearray support", "no");

if (PHP_BYTEARRAY != "no") {
	EXTENSION("bytearray", "bytearray.c");
}

